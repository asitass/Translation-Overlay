export const DEFAULTS = {
  CAPTURE_INTERVAL_IDLE: 3000,
  CAPTURE_INTERVAL_ACTIVE: 1500,
  OCR_LANGUAGES: ['eng', 'chi_sim'],
  OCR_CONFIDENCE_THRESHOLD: 60,
  OCR_DOWNSCALE: 0.65,
  TRANSLATION_PRIMARY: 'bergamot',
  TRANSLATION_FALLBACK: 'google',
  TRANSLATION_SOURCE_LANG: 'auto',
  TRANSLATION_TARGET_LANG: 'zh-CN',
  OVERLAY_FONT_SIZE: 14,
  OVERLAY_BACKGROUND_OPACITY: 0.75,
  OVERLAY_DISPLAY_MODE: 'sideBySide' as const,
  CACHE_DB_PATH: 'translation_cache.db',
  CACHE_MAX_AGE_HOURS: 168,
  OLLAMA_BASE_URL: 'http://localhost:11434',
  OLLAMA_MODEL: 'qwen2.5:3b',
  BERGAMOT_MODEL_DIR: 'bergamot-models',
  CHANGE_DETECTOR_THRESHOLD: 0.02,
  CHANGE_DETECTOR_BLOCK_SIZE: 64,
  CHANGE_DETECTOR_TIME_WINDOW_MS: 10000,
  CHANGE_DETECTOR_POSITION_GRID: 10,
  OCR_PREPROCESSING_ENABLED: true,
  OCR_PREPROCESSING_UPSCALE: 1.0,
  OCR_PREPROCESSING_GRAYSCALE: true,
  OCR_PREPROCESSING_NORMALIZE: true,
  OCR_GROUPING_ENABLED: true,
  OCR_GROUPING_VERTICAL_THRESHOLD_RATIO: 1.0,
  OCR_GROUPING_HORIZONTAL_THRESHOLD: 50,
  OCR_GROUPING_REQUIRE_OVERLAP: false,
  OCR_GROUPING_PARAGRAPH_GAP_RATIO: 2.0,
  OCR_GROUPING_DETECT_COLUMNS: true,
  OVERLAY_MAX_DISPLACEMENT_PX: 100,
  OVERLAY_MAX_SCREEN_RATIO: 0.3,
  STABILITY_LOCK_THRESHOLD: 3,
  UNLOCK_CHANGE_THRESHOLD: 0.15,
  TRANSLATION_QUALITY_THRESHOLD: 0.3,
  FUZZY_MATCH_THRESHOLD: 0.85,
  FUZZY_SEARCH_GRID_RANGE: 1,
  MIN_TEXT_LENGTH_FOR_FUZZY: 4,
  LOCK_CONSECUTIVE_FRAMES: 2,
  DYNAMIC_SIMILARITY_THRESHOLD: 0.85,
  DEFAULT_LOCK_MODE: 'document' as const,

  // Document mode improvements — faster lock + minimum display times
  DOCUMENT_STABILITY_LOCK_THRESHOLD: 1,    // 文档模式：1帧稳定即lock（vs 默认3帧）
  STABILITY_SIMILARITY_THRESHOLD: 0.4,     // 稳定性Jaccard相似度阈值（降低以适应OCR波动）
  MIN_LOCK_DISPLAY_TIME_MS: 5000,           // 锁定后至少保持5秒不解锁
  OVERLAY_MIN_DISPLAY_TIME_MS: 3000,        // overlay翻译至少显示3秒不替换
} as const;
